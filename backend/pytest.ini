[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=backend --cov-report=html --cov-report=term-missing --cov-fail-under=50
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests
